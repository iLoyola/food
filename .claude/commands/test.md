Run the full test suite for this project — unit tests, integration tests, and E2E tests — then summarise the results.

Steps:
1. Run `npx vitest run` and capture the output. Note how many test files passed/failed and the total test count.
2. Run `npx playwright test` and capture the output. Note how many E2E tests passed/failed.
3. Report a clear summary: overall pass/fail status, counts for each suite, and any specific failures with the test name and error message.

If anything fails, identify the most likely cause and suggest a fix.
